import edu.duke.*;
import java.util.*;
import java.util.regex.Matcher;
import java.util.regex.Pattern;
/**
 * Write a description of deleteAfterTesting here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class deleteAfterTesting {
public boolean List(char ch){
List<char[]> myList = Arrays.asList(new char[]{'a','e','i','o','u'});

myList.forEach (e -> ? (ch == (e))(true:false));
}
}